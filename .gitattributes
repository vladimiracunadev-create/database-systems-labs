# Los generadores escriben siempre LF. Sin esta normalizacion, un clon en
# Windows recibiria CRLF y `build_classes.py --check` fallaria en local
# comparando finales de linea en vez de contenido.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
